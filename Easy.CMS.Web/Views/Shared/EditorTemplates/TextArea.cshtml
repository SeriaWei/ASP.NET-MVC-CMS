@using Easy.Web.Metadata
@model String
@{
    var meteData = ViewData.ModelMetadata as EasyModelMetaData;
    var attrs = meteData.GetAttributes();
    if (!attrs.ContainsKey("rows"))
    {
        attrs.Add("rows", 5);
    }
}
@Html.TextArea("", Model, attrs)